spring:
  application:
    name: backend

  output:
    ansi:
      enabled: always

  mongodb:
    auto-index-creation: true

  #jackson:
    #serialization:
      #writeDatesAsTimestamps: false
  #devtools:
  #  restart:
  #    enabled: false
server:
  port: ${ZENDR_BACKEND_PORT}

logging:
  level:
    root: INFO